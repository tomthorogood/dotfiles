{
    "name": "Dev Container with jq and yq",
    "image": "ubuntu:latest",
    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "runArgs": [ "-v", "/var/run/docker.sock:/var/run/docker.sock" ],
    "postCreateCommand": "apt-get update && apt-get install -y jq && wget https://github.com/mikefarah/yq/releases/download/v4.13.5/yq_linux_amd64 -O /usr/bin/yq && chmod +x /usr/bin/yq",
    "extensions": []
}